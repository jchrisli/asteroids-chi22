import React, {useContext} from 'react'
import {Button} from '@material-ui/core';
// import {SocketContext} from '../SocketContext';

const Notifications = () => {
    // const {addPeer, call, receive} = useContext(SocketContext);
    return (
        <>
            {/* {call.isreceiveCall && !receive && (
                <div style={{display: 'flex', justifyContent: 'center'}}>
                    <h1> {call.name} is createPeer </h1>
                    <Button variant="contained" color="primary" onClick={addPeer}>
                        Pick up
                    </Button>
                </div>
            )} */}
        </>
    );
};

export default Notifications